const { resolve } = require('path');

module.exports = {
  config: resolve('src', 'shared', 'database', 'config', 'database.js'),
  'models-path': resolve('src', 'models'),
  'seeders-path': resolve('src', 'shared', 'database', 'seeders'),
  'migrations-path': resolve('src', 'shared', 'database', 'migrations'),
}